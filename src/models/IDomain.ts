import IRectangleCoordinates from "./IRectangleCoordinates";

interface IDomain {
    id: string,
    name: string
    code: string
    dataType: string,
    length: number
}

export default IDomain;
